#pragma once

const float PI = 3.141593f;

enum team
{
    NONE = 0,
    BLUE,
    YELLOW
};

const float tableLength = 300.f; //en cm
const float tableWidth = 200.f;

const float gobeletRadius = 3.5f;
const float robotRadius = 15.f;